# Netlify config for the Next.js static export.
# The site builds to `out/` (plain static HTML/CSS/JS) and deploys with no runtime.
[build]
  command = "yarn build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
